<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yi.current.mapper.dao.BaikeMapper">
    <select id="selectByBookName" resultType="com.yi.current.mapper.model.Baike">
        select * from baike where book = #{book}
    </select>
</mapper>